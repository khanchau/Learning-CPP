#include <iostream>
using std::cout;


/*
    Iterates through a given array to see if contents
    add up to a specified target value.
    @param arr[]: array to search content of.
    @param target: integer value to check sums of array contents against.
    @return: An array containing the indices of the array that add up to
             the target.

    6/7/20 - Looking into vectors instead of arrays.  
*/
int twosums(int arr[], int target) {
    return 0;
}

int main() {
    int arr[] = {0,1,2,3};
    twosums(arr, 5);
    return 0;
}